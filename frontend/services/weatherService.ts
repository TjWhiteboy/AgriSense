import { apiClient } from './apiClient';

export const weatherService = {
  // Add methods here later when backend has explicit weather routes
  // For example:
  // getWeatherByDistrict: async (district: string) => apiClient.get(`/weather?district=${district}`),
};
